server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location = /nourhan {
        try_files /nourhan.html =404;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}